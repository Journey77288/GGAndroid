apply from: "${rootProject.projectDir}/gradle/config/module.gradle"
dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    api project(':core:service:pay:pay-core')
    api libs.wechat_sdk
    implementation libs.gson
}
