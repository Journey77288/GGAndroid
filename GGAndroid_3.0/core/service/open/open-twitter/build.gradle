apply from: "${rootProject.projectDir}/gradle/config/module.gradle"

dependencies {
    api fileTree(include: ['*.jar'], dir: 'libs')
    api project(':core:service:open:open-core')
    api(libs.twitter_sdk) { transitive = true }
    api(libs.tweet_composer) { transitive = true }
    api libs.tweet_ui
}
